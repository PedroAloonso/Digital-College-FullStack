generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:@localhost:3306/test_db"
  
}

model Users {
    id Int @id @default(autoincrement())
    name String
    email String @unique
}